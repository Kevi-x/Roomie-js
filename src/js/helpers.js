/**
 * 
 * @param {number} min lower boundry of the range
 * @param {number} max upper boundry of the range
 * @returns random number whitin the range 
 */
export const getRandom = (min, max) => Math.floor(Math.round()*(max-min)+min);